#include <iostream>
#include <stdio.h>
#include <stdlib.h>

void merge(int*, int*, int, int, int);

void mergeSort (int*, int*, int, int); 

void mergeWOkey (double*, int, int, int);

void mergeSortWOkey (double*, int, int);
